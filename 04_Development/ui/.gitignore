node_modules
dist
storybook-static
*.log
.DS_Store
# Generated token CSS is committed so the app builds without a prior token run in review,
# but it is regenerated by `npm run build:tokens`. Kept in tree intentionally.
